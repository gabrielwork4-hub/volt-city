---
interface Props { kicker?: string; title: string; lead?: string; }
const { kicker, title, lead } = Astro.props;
---
<header class="section-heading">{kicker && <p class="kicker">{kicker}</p>}<h2>{title}</h2>{lead && <p class="lead">{lead}</p>}</header>
